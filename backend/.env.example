# Firebase Service Account (paste the entire JSON as a single line, or use GOOGLE_APPLICATION_CREDENTIALS path)
FIREBASE_SERVICE_ACCOUNT={"type":"service_account","project_id":"..."}

# Or use file path instead
# GOOGLE_APPLICATION_CREDENTIALS=./service-account.json

# Anthropic API Key (for Claude fallback parsing)
ANTHROPIC_API_KEY=sk-ant-...

# Google Places API Key (moved from frontend for security)
GOOGLE_PLACES_API_KEY=AIzaSy...

# SendGrid Webhook Verification (optional but recommended)
SENDGRID_WEBHOOK_VERIFICATION_KEY=

# Server
PORT=3001
NODE_ENV=development

# Frontend URL (for CORS)
FRONTEND_URL=http://localhost:5173
